<article>
  <h1><%= @article.categories.map(&:name).join(', ') %></h1>
  <h3><%= @article.title %></h3>
  <p><%= @article.tags.map(&:name).join(', ') %></p>
  <hr/>
  <p><%= @article.description %></p>
</article>

